export const listSelector = (state) => state.list

export const processSelector = (state) => state.process

export const sortSelector = (state) => state.sort

export const currentStepSelector = (state) => state.currentStep

export const speedSelector = (state) => state.speed

export const firstRunSelector = (state) => state.firstRun
